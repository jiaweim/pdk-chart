# 连续 Color Scales 以及 Color Bar

## 简介

如何在 scatter, bar, map 以及 heatmap 中新建、设置和调整连续色阶与颜色条。

## PaintScale

`PaintScale` 定义数值与颜色之间的映射。

### GradientPaintScale

`GradientPaintScale` 为 `PaintScale` 的实现类，在颜色之间执行差值运算。



